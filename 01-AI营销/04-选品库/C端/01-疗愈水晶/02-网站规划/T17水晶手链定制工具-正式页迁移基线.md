# T17 正式页迁移基线

记录时间：2026-07-13

## 正式页事实

- 页面 ID：`50236`
- URL：`https://goearthward.com/tools/crystal-bracelet-builder/`
- slug：`crystal-bracelet-builder`
- 当前状态：`publish`
- 当前正文：旧版内嵌工具，约 `1,033,135` 字符；包含已废弃的 3D / 全局 Bead Size / 旧交易逻辑。
- 必须保留：页面 ID、URL、slug、SEO 设置、canonical、Elementor 元数据和旧木托盘资产。

## 回滚基线

WordPress 在切换前已存在下列可回读 revision；最新基线为：

- revision ID：`51803`
- 修改时间：`2026-07-09T10:40:16`
- 正文长度：`1,033,135`

切换前重新读取该 revision。若新页面异常，只恢复该 revision 的正文，不删除旧原型、旧 HTML、旧 ZIP 或页面本身。

## 草稿页当前运行态

- 草稿页 ID：`54723`，需在已登录后台会话中使用 `?page_id=54723&preview=true` 访问。
- 2026-07-13 回读证明：当前已安装的 `0.1.5` 能渲染旧版 2D 编辑器，但 `[ew_t17_builder_landing]` 与 `[ew_t17_builder_seo]` 仍显示为原始短代码，官方设计区域也会列出普通 Woo 商品。
- 结论：草稿页仅用于确认部署后的 v3 验收，当前状态不能作为 v3 已完成或正式切换依据。

## 必须隔离的旧交易链路

- 正式页 `50236` 仍带有旧版 `t17-builder-v1 / 2026-07-02-mvp` 初始化包、旧 2D 和 Three.js 3D 画布、全局 Bead Size、`t17-step1-draft` 本地草稿，以及 `t17_add_custom_bracelet` AJAX Checkout。
- Code Snippets 中存在全局启用的 snippet `#20 T17 Crystal Bracelet Builder — WooCommerce custom add-to-cart`。它使用硬编码测试价、`t17_config` 购物车数据和旧 Woo hooks，不读取 v3 Material / Variant 数据库。
- 商品 `49026` 是已发布但目录隐藏的 `Custom Crystal Bracelet Test`，当前价格 `$0.00`，仅可视作旧测试承载商品；不能未经重建和订单验证直接用作 v3 生产承载商品。

正式切换前：先在草稿页完成 v3 订单测试；随后停用或严格限制 snippet `#20`，并让旧页面正文、Three.js、旧 AJAX 和旧本地草稿一起退出正式 URL。旧商品与代码均保留为历史回滚证据，不删除。

## 允许的正式切换

仅当 v3 草稿预览页完成真实素材、报价、购物车和测试订单验收后，才将页面 `50236` 的正文替换为：

```text
[ew_t17_builder_landing]
[ew_t17_official_designs limit="12"]
[ew_t17_bracelet_builder]
[ew_t17_builder_seo]
```

切换后回读 URL、页面状态、SEO/Elementor meta，并在移动端和桌面端进行视觉及结账验证。
