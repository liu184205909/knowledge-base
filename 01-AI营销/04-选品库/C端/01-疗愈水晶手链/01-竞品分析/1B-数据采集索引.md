# 1B 数据采集索引

> 用途：记录步骤1B所有结构化数据源、采集状态和后续处理状态。项目简报只保留入口链接；具体数据源状态以本文档为准。

## Google Sheets

| 数据源 | 内容 | 链接 | 当前状态 | 下一步 |
|------|------|------|---------|------|
| 竞品总表 | 40家竞品总表，含AS、Organic Traffic、traffic.cv渠道、建站平台、竞品类型、商业模式、深度拆解文档等字段 | [打开](https://docs.google.com/spreadsheets/d/1zcWFPw7lFq_L6aBpEEpBhU0FKf7uJi6hiQYCVoah_vA) | 已建立 | 按新增竞品或字段变化维护 |
| SEMrush-Top-Pages | 竞品热门页面；每个竞品一个原始工作表 | [打开](https://docs.google.com/spreadsheets/d/1x__cXM-FCmTe_BjTND0nG0VhWrjbVl8iFajtXGE854U) | 已建立 + 过滤版 TopPages_All 已生成 | 新增竞品工作表后重新运行 `generate_all_tables.py --top-pages` |
| SEMrush-Top-Keywords | 竞品关键词排名；每个竞品一个原始工作表 | [打开](https://docs.google.com/spreadsheets/d/18CqR8GzvsonO5zYUaodDPB3oiIcdjdeCVJZn5BaPJng) | 已建立 + 过滤版 TopKeywords_All + Keyword-Page-Proof 已生成 | 新增竞品工作表后重新运行 `generate_all_tables.py --top-keywords` + `generate_keyword_page_proof.py` |
| Seed Keywords | 关键词主表；4,391条，14列，已按过滤版Proof回填竞品验证 | [打开](https://docs.google.com/spreadsheets/d/1HhKDz7_LlY1V1_wMSCLn4-8ASoI64u6MuW7iOkKAFDc) | 已建立 + 竞品验证已回填 | Topic Cluster / Recommended Page Type 待 AI 聚类填充；新增竞品后重新运行 `backfill_seed_keywords.py` |
| 内容清单 | 709条内容选题草案 | [打开](https://docs.google.com/spreadsheets/d/1PjXmT3CDngnbi4kIHAZZg6_qj-4N5RpKxBKMtxGxl5M) | 草案已生成 | 补 KD/CPC/竞品验证字段后再批量 Brief |

## 汇总表与验证视图

| 名称 | 所属表格 | 作用 | 状态 | 生成规则 |
|------|---------|------|------|---------|
| `TopPages_All` | SEMrush-Top-Pages | 汇总所有竞品有流量证据的页面 | **已生成** (2026-05-22): 289行 × 9列 | 遍历所有竞品原始工作表，排除说明/汇总/分析表；默认只保留 `Traffic >= 100` 的页面；每竞品最多300页；追加 competitor_domain / source_sheet / normalized_url |
| `TopKeywords_All` | SEMrush-Top-Keywords | 汇总所有竞品有流量证据的关键词排名和排名URL | **已生成** (2026-05-22): 2,000行 × 12列 | 遍历所有竞品原始工作表，排除说明/汇总/分析表；默认只保留 `Traffic > 0` 或 `Traffic(%) > 0`、有URL、且英文词数不少于2的关键词行；每竞品最多2000词；追加 competitor_domain / source_sheet / normalized_url |
| `Keyword-Page-Proof` | SEMrush-Top-Keywords | 判断某个关键词是否已有竞品页面实际拿到流量 | **已生成** (2026-05-22): 1,913行 × 12列；Strong(299) / Medium(1,614) / Weak(0) / None(0) | `TopKeywords_All.competitor_domain + normalized_url` JOIN `TopPages_All.competitor_domain + normalized_url`；只保留关键词流量和页面流量都大于0的记录；范围限定为 Seed Keywords 命中词 + 非主表 Strong/Medium 增量机会 |

## Seed Keywords 回填状态

| 字段 | 状态 | 说明 |
|------|------|------|
| Topic Cluster | 待填充 | 需要 AI 聚类分析后填充 |
| Recommended Page Type | 待填充 | 需要 AI 意图分析后填充 |
| Priority | **已填充** | P0(13) / P1(1,103) / P2(3,175) / P3(100)，基于 Volume/KD/CPC/Proof 计算 |
| Source Type | **已填充** | Seed / Seed + Competitor Proof |
| Source Detail | **已填充** | 竞品域名（有验证的） |
| Competitor Proof | **已填充** | 277条有验证（6.3%）：Strong/Medium，基于每竞品2000词上限后的 Keyword-Page-Proof |
| Proof URL | **已填充** | 竞品实际排名URL |

## URL 标准化规则

`normalized_url` 必须统一：

- 小写
- 去掉 `http://` 或 `https://`
- 去掉 `www.`
- 去掉查询参数 `?`
- 去掉锚点 `#`
- 去掉末尾 `/`

同一路径可能出现在多个竞品中，关联时必须使用 `competitor_domain + normalized_url`，不能只用 URL 路径。

## 1C 使用规则

- Seed Keywords 是本项目唯一关键词主表。
- Top Pages / Top Keywords 不直接合并进关键词主表。
- `TopPages_All` / `TopKeywords_All` 是过滤后的证据索引，不是原始数据全量备份。
- `Keyword-Page-Proof` 只负责给关键词主表补竞品成功证据，不能把关键词流量为0的排名行当成成功案例。
- 竞品验证中发现的增量词，补回 Seed Keywords 主表，并标记 `Source Type` / `Source Detail`。

## 当前缺口

- Topic Cluster / Recommended Page Type 待 AI 聚类填充。
- 内容清单 709 条仍是草案，KD/CPC 和跨品类关键词数据未完全补齐。
- 当前仅 1 家竞品（thecrystalcouncil）数据；新增竞品工作表后需重新运行汇总、Proof 和 Seed Keywords 回填脚本。

## 维护脚本

| 脚本 | 用途 | 运行时机 |
|------|------|---------|
| `generate_all_tables.py --top-pages` | 生成/刷新 TopPages_All | 新增竞品 Top Pages 工作表后 |
| `generate_all_tables.py --top-keywords` | 生成/刷新 TopKeywords_All | 新增竞品 Top Keywords 工作表后 |
| `generate_keyword_page_proof.py` | 生成/刷新 Keyword-Page-Proof | TopKeywords_All 或 TopPages_All 刷新后 |
| `backfill_seed_keywords.py` | 回填 Seed Keywords 竞品验证 | Keyword-Page-Proof 刷新后 |
